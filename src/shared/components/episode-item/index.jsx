import React from "react";
import PropTypes from "prop-types";

const EpisodeItem = ({ data }) => {
  return <div key={data.id}>ItemData</div>;
};

EpisodeItem.propTypes = {};

export default EpisodeItem;
