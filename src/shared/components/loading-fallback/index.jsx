/** `LoadingFallback` -Renders fallback UI for suspense */
const LoadingFallback = () => {
  return (
    <section className="bg-primary-white h-screen flex justify-center items-center text-primary-dark text-4xl">
      Loading ...
    </section>
  );
};

export default LoadingFallback;
